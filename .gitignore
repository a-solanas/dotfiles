# Local issue tracker — not for version control (migrates to GitHub Issues when ready)
.scratch/

# Visual Studio Code workspace settings (local, not config)
.vscode/

# Local reference copy of old dotfiles
old_dotfiles/

# Fish runtime state — machine-local, not config
stow/fish/.config/fish/fish_variables
