# Tips

- All configs are symlinked from `~/.dotfiles/stow/`
- Edit configs directly, changes reflect immediately
- Fish is default shell in WezTerm only
- System shell remains zsh (login shell)
- Use `z` instead of `cd` for smarter navigation
- Abbreviations show actual command before executing
